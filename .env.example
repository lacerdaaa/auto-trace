# Copie este arquivo para .env e ajuste os valores

DATABASE_URL="postgresql://postgres:1234@localhost:5432/postgres?schema=public"

# Porta da API (padrão 3333)
PORT=3333

# JWT
JWT_SECRET="auto-trace-local-secret"
JWT_EXPIRES_IN="1d"

# Km/mês padrão quando não houver histórico suficiente
AVERAGE_MONTHLY_KM=1000

# Diretório base para uploads (opcional)
# UPLOAD_ROOT=./uploads

